@commands.hybrid_command( name="bagperson", description="Will make a bag out of you", ) async def bagperson(self, context: Context): """ This is a testing command that does nothing. :param context: The application command context. """ # disc_image = context.user.avatar img_file = "lootboi.jpg" rgb_image = Image.open(img_file) rgba_image = rgb_image.convert('RGBA') rgba_image.resize() rgba_image.save("lootboi.png") img = Image.open("lootboi.png") border = 256 alpha = Image.new('L', (1024-2*border,1024-2*border), "white") alpha = ImageOps.expand(alpha, border) dem_fps = ["lootboi.png"] # Open each image in turn and push in our ready-made alpha channel for item in dem_fps: im = Image.open(item).convert('RGBA') im.putalpha(alpha) im.save(f'RESULT-{item}') alpha.save("testpics/pillowtest.png") response = openai.Image.create_edit( image=open("lootboi.png", "rb"), # mask=open('piglets-now2.png', "rb"), prompt="inside an extensive sewer system that has wires and other conspicuous devices", n=1, size='512x512', ) image_url = response['data'][0]['url'] await context.send(image_url)